<UserControl x:Class="WpfApp.MVVM.Views.AnotherView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WpfApp.MVVM.Views"
             xmlns:viewModels="clr-namespace:WpfApp.MVVM.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance viewModels:AnotherVm}">
  <StackPanel Orientation="Vertical">
    <TextBlock Text="Another View" HorizontalAlignment="Center" FontSize="22" />
    <Button Margin="0 10 0 0"
            Width="200"
            Content="Home View"
            Command="{Binding NavigateHomeViewCommand}" />
  </StackPanel>
</UserControl>

